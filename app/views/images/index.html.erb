<p id="notice"><%= notice %></p>

<h1><%=@tag.titlecase%> Images</h1>

  <div class="gallery">
    <% @images.each do |image| %>
      <%= link_to image_tag(image.file.url(:thumb)), image %> 
    <% end %>
  </div>


